<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Balance_Leave__c</fullName>
    <externalId>false</externalId>
    <formula>IF(First_Day_Of_Month__c == Joining_Date__c, ((YEAR(TODAY()) - YEAR(Joining_Date__c) - 1) *12) + (12 - MONTH(Joining_Date__c) +1) + MONTH(TODAY())
, ((YEAR(TODAY()) - YEAR(Joining_Date__c) - 1) *12) + (12 - MONTH(Joining_Date__c) +1) + MONTH(TODAY())
- 1)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Balance Leave</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
